import type { Episode } from "@/types";
import { episode1 } from "./episode1";

/**
 * EPISODE REGISTRY
 * ================
 * Add new episodes here. They play in order.
 * Create a new file (episode2.ts, episode3.ts, etc.) and import it below.
 */
export const EPISODES: Episode[] = [
  episode1,
  // episode2,
  // episode3,
];
